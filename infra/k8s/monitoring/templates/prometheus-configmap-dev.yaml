{{- if eq .Values.global.environment "dev" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-monitoring-infra-d-prometheus-config
  namespace: monitoring
  labels:
    app.kubernetes.io/name: prometheus
    app.kubernetes.io/instance: cluster-monitoring-infra-dev
data:
  prometheus.yml: |
    global:
      scrape_interval: 30s
      evaluation_interval: 30s

    scrape_configs:
      # 1. Scrape Prometheus itself
      - job_name: 'prometheus'
        static_configs:
          - targets: ['localhost:9090']

      # 2. Fixed: kubernetes-apiservers (proper auth for in-cluster)
      - job_name: 'kubernetes-apiservers'
        kubernetes_sd_configs:
          - role: endpoints
            namespaces:
              names:
                - default
        scheme: https
        tls_config:
          ca_file: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
          insecure_skip_verify: true
        bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
        relabel_configs:
          - source_labels: [__meta_kubernetes_service_name, __meta_kubernetes_endpoint_port_name]
            action: keep
            regex: kubernetes;https

      # 3. Fixed: kubernetes-nodes (scrape kubelet via API server proxy)
      - job_name: 'kubernetes-nodes'
        kubernetes_sd_configs:
          - role: node
        scheme: https
        tls_config:
          ca_file: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
          insecure_skip_verify: true
        bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
        relabel_configs:
          - action: labelmap
            regex: __meta_kubernetes_node_label_(.+)
          - target_label: __address__
            replacement: kubernetes.default.svc:443
          - source_labels: [__meta_kubernetes_node_name]
            target_label: __metrics_path__
            replacement: /api/v1/nodes/${1}/proxy/metrics
          - source_labels: [__meta_kubernetes_node_name]
            target_label: instance
      # 4. CoreDNS (dedicated job, correct port 9153)
      - job_name: 'coredns'
        kubernetes_sd_configs:
          - role: endpoints
          - role: service
        relabel_configs:
          # Only scrape kube-dns / coredns services
          - source_labels: [__meta_kubernetes_service_name]
            action: keep
            regex: kube-dns|coredns
          # Only port 9153
          - source_labels: [__meta_kubernetes_endpoint_port_number]
            action: keep
            regex: "9153"
          # Set metrics path
          - target_label: __metrics_path__
            replacement: /metrics
          # Namespace and pod labels
          - source_labels: [__meta_kubernetes_namespace]
            action: replace
            target_label: kubernetes_namespace
          - source_labels: [__meta_kubernetes_pod_name]
            action: replace
            target_label: kubernetes_pod_name
      # 5. kubernetes-service-endpoints (only annotated services, exclude CoreDNS)
      - job_name: 'kubernetes-service-endpoints'
        kubernetes_sd_configs:
          - role: endpoints
        relabel_configs:
          # Only scrape services with prometheus.io/scrape: "true"
          - source_labels: [__meta_kubernetes_service_annotation_prometheus_io_scrape]
            action: keep
            regex: "true"
          # Exclude CoreDNS (kube-dns) — it has its own coredns job
          - source_labels: [__meta_kubernetes_service_name]
            action: drop
            regex: kube-dns|coredns
          # Use prometheus.io/path if present
          - source_labels: [__meta_kubernetes_service_annotation_prometheus_io_path]
            action: replace
            target_label: __metrics_path__
            regex: (.+)
          # Use prometheus.io/port if present
          - source_labels: [__meta_kubernetes_service_annotation_prometheus_io_port]
            action: replace
            target_label: __address__
            regex: ([0-9]+)
            replacement: ${1}
          # Label map
          - action: labelmap
            regex: __meta_kubernetes_service_label_(.+)
          # Namespace and service name
          - source_labels: [__meta_kubernetes_namespace]
            action: replace
            target_label: kubernetes_namespace
          - source_labels: [__meta_kubernetes_service_name]
            action: replace
            target_label: kubernetes_service
      # 6. Loki (explicit job, only port 3100)
      - job_name: 'loki'
        kubernetes_sd_configs:
          - role: pod
        relabel_configs:
          # Only scrape pods with app=loki
          - source_labels: [__meta_kubernetes_pod_label_app]
            action: keep
            regex: loki
          # Only scrape port 3100
          - source_labels: [__meta_kubernetes_pod_container_port_number]
            action: keep
            regex: "3100"
          # Set metrics path
          - target_label: __metrics_path__
            replacement: /metrics
          # Map namespace and pod name
          - source_labels: [__meta_kubernetes_namespace]
            action: replace
            target_label: kubernetes_namespace
          - source_labels: [__meta_kubernetes_pod_name]
            action: replace
            target_label: kubernetes_pod_name      
{{- end }}