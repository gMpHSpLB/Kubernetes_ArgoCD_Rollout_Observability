{{- if eq .Values.global.environment "dev" }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: cluster-monitoring-infra-grafana
  namespace: monitoring
rules:
  - apiGroups: [""]
    resources: ["configmaps", "secrets"]
    verbs: ["get", "list", "watch"]
{{- end }}